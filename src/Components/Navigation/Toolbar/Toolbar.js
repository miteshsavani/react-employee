import React from 'react';
import 

const toolbar = (props) => (
    <header>
            <div> MENU </div>
            <div> Logo </div>
            <nav>
                ...
            </nav>
    </header>
);

export default toolbar;